# emberkeep
Unity 6 tech demo: every NPC runs a quantized Llama-3.2-3B locally via a custom llama.cpp native plugin. Worker-thread inference with a per-frame token budget keeps the render loop at 60 FPS. Behavior-tree + LLM hybrid NPCs, persistent memory, streaming dialogue.
